in order for the these to work, the db is an sqlite,
it was created with the following :

create table colors(id text, color text);

where id can be anything
and color should be of format 'xxxxxx'
where each 'x' is a value that should be between 0-9 and a-f
representing RGB colors
